{
  "name": "@monaco-ui/components",
  "version": "1.1.5",
  "private": true,
  "main": "src/index.tsx",
  "typings": "core/index.d.ts",
  "files": [
    "core"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf ./core && rm tsconfig.tsbuildinfo",
    "test": "jest --env=jsdom --passWithNoTests"
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
